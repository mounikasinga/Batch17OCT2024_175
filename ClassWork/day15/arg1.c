/*write a program to read  the content of  employee store them  command line args*/
#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main(int argc, char *argv[]) {
	for(i=0;i<argc;i++)
	if (argc != 4) {
        printf("Id=%d\n", argv[i]);
	printf("Name=%s",argv[i]);
        return 1;
    }


